# develOS
A community-driven operating system built collaboratively by developers and enthusiasts.

# How to run
install a toolchain like x86_64-elf-gcc, nasm and qemu
then build & run the OS with:
```
make clean
make fetchDeps
make disk
make
make run
```
